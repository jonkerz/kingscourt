h4 {{selectedCard.name || "Select a card"}}
div ng-if="!selectedCard"
  .black_card.shadow
    img.card_image width="180" ng-src="cards/Randomizer.jpg"

div ng-if="selectedCard"
  .black_card.shadow
    img.card_image width="180" ng-src="cards/{{selectedCard.image}}"
  Expansion:
  img ng-src="icons/{{selectedCard.expansion}}.png" width="16" height="16"
  {{selectedCard.text}}
  Cost:
    img ng-src="icons/16px-Coin{{selectedCard.cost}}.png"
